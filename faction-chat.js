(()=>{'use strict';const status=document.querySelector('#status'),panel=document.querySelector('#panel'),box=document.querySelector('#messages');let account,before,channel,busy=false;const paint=(rows,prepend=false)=>{const online=TODMPresence.getOnlineIds(),html=[...rows].reverse().map(m=>`<div class="message-row ${m.user_id===account.user.id?'mine':''}">${TODMSocialUI.avatar({...m,online:online.includes(m.user_id)})}<div class="bubble">${m.deleted?'Сообщение удалено модератором':TODMSocialUI.esc(m.body)}<small>${TODMSocialUI.esc(m.display_name)} · ${new Date(m.created_at).toLocaleString('ru-RU')} ${TODMSocialUI.badges(m)}</small></div></div>`).join('');prepend?box.insertAdjacentHTML('afterbegin',html):box.innerHTML=html;if(rows.length)before=rows[rows.length-1].id;if(!prepend)box.scrollTop=box.scrollHeight};const load=async prepend=>{const{data,error}=await TODMAuth.client.rpc('faction_message_list',{p_before:prepend?before:null});if(error)throw error;paint(data||[],prepend)};const init=async()=>{account=await TODMAuth.getAccount();if(!account.session){location.replace('Auth.html?next=FactionChat.html');return}if(!account.profile?.selected_faction)throw Error('Сначала выберите фракцию в личном кабинете.');document.querySelector('#title').textContent=`Чат · ${TODMSocialUI.factions[account.profile.selected_faction]}`;await load(false);panel.hidden=false;status.textContent='';channel=TODMAuth.client.channel(`faction-${account.profile.selected_faction}`).on('postgres_changes',{event:'*',schema:'public',table:'faction_messages',filter:`faction=eq.${account.profile.selected_faction}`},()=>load(false)).subscribe()};document.querySelector('#older').onclick=()=>load(true).catch(e=>status.textContent=TODMAuth.message(e));document.querySelector('#compose').onsubmit=async e=>{e.preventDefault();if(busy)return;busy=true;const body=document.querySelector('#body'),{error}=await TODMAuth.client.rpc('faction_message_send',{p_body:body.value});busy=false;if(error)return status.textContent=TODMAuth.message(error);body.value='';await load(false)};addEventListener('beforeunload',()=>channel&&TODMAuth.client.removeChannel(channel));(window.TODMAuth?init():new Promise(r=>addEventListener('todm-auth-ready',r,{once:true})).then(init)).catch(e=>status.textContent=TODMAuth.message(e))})();
