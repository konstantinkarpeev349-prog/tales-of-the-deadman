(()=>{'use strict';
const names={dokains:'Докаинов',frauster:'Фраустера',maizervin:'Майзервина',paladins:'Паладинов',doronto:'Культа Доронто'};
const init=async()=>{try{const session=await TODMAuth.getSession();if(!session)return;const{data,error}=await TODMAuth.client.from('faction_leader_notifications').select('id,faction').is('seen_at',null).order('created_at',{ascending:true}).limit(1).maybeSingle();if(error||!data)return;const style=document.createElement('link');style.rel='stylesheet';style.href='leadership-notification.css';document.head.append(style);const modal=document.createElement('div');modal.className='leadership-congratulation';modal.setAttribute('role','dialog');modal.setAttribute('aria-modal','true');modal.setAttribute('aria-labelledby','leader-congratulation-title');modal.innerHTML=`<div class="leadership-scroll"><span class="leadership-seal">TODM</span><p class="leadership-eyebrow">Указ Архива</p><h2 id="leader-congratulation-title">Архив поздравляет вас</h2><p>Отныне вы — лидер фракции <strong>${names[data.faction]||data.faction}</strong>.</p><blockquote>Но помните: короли не правят вечно…</blockquote><button type="button">Принять лидерство</button></div>`;document.body.append(modal);const button=modal.querySelector('button');button.focus();button.onclick=async()=>{const{error:ackError}=await TODMAuth.client.from('faction_leader_notifications').update({seen_at:new Date().toISOString()}).eq('id',data.id).eq('user_id',session.user.id);if(!ackError)modal.remove()}}catch(e){console.warn('TODM leader notification unavailable',e)}};
if(window.TODMAuth)init();else addEventListener('todm-auth-ready',init,{once:true});
})();
