(()=>{"use strict";const fallback=value=>{const field=document.createElement("textarea");field.value=value;field.setAttribute("readonly","");field.style.position="fixed";field.style.opacity="0";document.body.append(field);field.select();const copied=document.execCommand("copy");field.remove();if(!copied)throw new Error("copy failed")};document.addEventListener("click",async event=>{const button=event.target.closest("[data-copy]");if(!button)return;const label=button.querySelector("span"),original=label.textContent;try{if(navigator.clipboard?.writeText)await navigator.clipboard.writeText(button.dataset.copy);else fallback(button.dataset.copy);label.textContent="Скопировано"}catch{try{fallback(button.dataset.copy);label.textContent="Скопировано"}catch{label.textContent="Выделите адрес вручную"}}button.classList.add("copied");setTimeout(()=>{label.textContent=original;button.classList.remove("copied")},2200)})})();
